using UnityEngine;

public class SoundController : MonoBehaviour
{
  // Would hook up directly to the input system as its own context. functions like a remote controller

  // needs to be a singleton.
}
